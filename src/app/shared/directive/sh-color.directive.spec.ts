import { ShColorDirective } from './sh-color.directive';

describe('ShColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ShColorDirective();
    expect(directive).toBeTruthy();
  });
});
